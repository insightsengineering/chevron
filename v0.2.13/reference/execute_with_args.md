# Execute function with given arguments

Execute function with given arguments

## Usage

``` r
execute_with_args(fun, ...)
```

## Details

If the function has `...`, this function will not pass other arguments
to `...`. Only named arguments are passed.
