# Prune table except specified levels

Prune table except specified levels

## Usage

``` r
prune_except(keep)
```

## Arguments

- keep:

  (`character`) levels to keep.

## Value

A pruning `function`.
