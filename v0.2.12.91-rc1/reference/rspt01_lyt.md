# `rspt01` Layout

`rspt01` Layout

## Usage

``` r
rspt01_lyt(
  arm_var,
  rsp_var,
  ref_group,
  odds_ratio,
  perform_analysis,
  strata,
  conf_level,
  methods
)
```

## Arguments

- arm_var:

  (`string`) variable used for column splitting
