# Prune table up to an `ElementaryTable`

Avoid returning `NULL` when the `table` is empty.

## Usage

``` r
smart_prune(tlg)
```

## Arguments

- tlg:

  (`TableTree`) object.

## Value

pruned `TableTree`.
