# Fuse list elements

Fuse list elements

## Usage

``` r
fuse_sequentially(x, y)
```

## Arguments

- x:

  (`list`) to fuse.

- y:

  (`list`) to fuse. Elements with names already existing in `x` are
  discarded.
